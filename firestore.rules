rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir acesso completo a todas as coleções no ambiente de teste/preview do AI Studio
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
